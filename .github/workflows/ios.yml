name: iOS
on:
  push:
    paths: ['ios/**', 'Assets/radar-map.html', 'Assets/us-states.geo.json', 'Assets/1024.png']
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      # Copy pre-made 1024x1024 icon into the asset catalog
      - name: Copy app icon
        run: cp Assets/1024.png ios/Drizzle/Assets.xcassets/AppIcon.appiconset/icon-1024.png

      - name: Build
        run: |
          xcodebuild -project ios/Drizzle.xcodeproj \
            -scheme Drizzle \
            -configuration Release \
            -sdk iphoneos \
            -archivePath build/Drizzle.xcarchive \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_ALLOWED=NO \
            archive

      - uses: actions/upload-artifact@v4
        with:
          name: drizzle-ios
          path: build/Drizzle.xcarchive
